name: Discord Release Notification

on:
  release:
    types: [published]

# Releases created inside GitHub Actions with the default GITHUB_TOKEN do not
# trigger this workflow. Automated tag releases send Discord notifications from
# release.yml; this workflow remains as the path for manual or external release
# publications.

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Send Discord notification
        uses: SethCohen/github-releases-to-discord@v1.19.0
        with:
          webhook_url: ${{ secrets.WEBHOOK_URL }}
          color: "2105893"
          username: "UCP Release"
          avatar_url: "https://raw.githubusercontent.com/mflRevan/unity-control-protocol/main/assets/branding/ucp-icon-128.png"
          footer_title: "Unity Control Protocol"
          footer_timestamp: true
          reduce_headings: true
